# 보안 정책 (Security Policy)

한빛교회 홈페이지는 교인들의 개인정보(이름·연락처·기도제목·활동 기록)를 다루는 서비스입니다.
취약점 제보를 환영하며, 아래 절차를 따라 주시면 신속히 대응하겠습니다.

## 지원 범위 (Supported Versions)

이 프로젝트는 버전 릴리스 대신 브랜치 기반으로 운영됩니다.

| 대상 | 지원 여부 |
| --- | --- |
| `main` (배포 스냅샷) | ✅ 보안 수정 대상 |
| `develop` (개발 브랜치) | ✅ 보안 수정 대상 — 수정은 항상 develop 에 먼저 반영 |
| 포크 원본(dasida)에서 갈라진 과거 이력 | ❌ 지원하지 않음 |

## 취약점 신고 방법 (Reporting a Vulnerability)

**공개 이슈로 올리지 말아 주세요.** 수정 전 공개는 실제 교인 데이터를 위험에 빠뜨릴 수 있습니다.

1. **GitHub 비공개 취약점 신고(권장)** — 저장소의 **Security → Report a vulnerability** 에서
   비공개 어드바이저리로 제보해 주세요.
2. 신고에는 가능하면 다음을 포함해 주세요:
   - 영향 범위(어떤 데이터/권한이 노출·우회되는지)
   - 재현 절차(요청 예시, 계정 권한 전제)
   - 발견한 환경(브랜치/커밋, 로컬 또는 배포)

### 대응 약속

- **72시간 이내** 접수 확인 및 초기 심각도 평가를 회신합니다.
- 유효한 취약점은 심각도에 따라 우선 수정하며, 진행 상황을 어드바이저리 스레드로 공유합니다.
- 수정이 배포되기 전까지 상세 내용은 비공개로 유지해 주세요. 수정 후 크레딧을 원하시면 명시해 드립니다.

## 관심 영역 (In Scope)

- 인증·세션: JWT/httpOnly 쿠키, 2FA(TOTP), 세션 해지·denylist 우회
- 인가: 역할(ADMIN/OPERATOR/…) 우회, 관리자 API(`/api/admin/**`) 접근 통제
- 개인정보 노출: 익명 기도제목 작성자 역추적, 교인만 공개(MEMBERS) 우회, 회원 열거
- 업로드: 이미지/문서 업로드 검증 우회, 메타데이터(EXIF GPS) 잔존
- 인젝션류: XSS(리치 본문), SQLi, CSV 수식 인젝션, SSRF
- 브루트포스·rate limit 우회(로그인/2FA/상호작용)

## 범위 외 (Out of Scope)

- 서비스 거부(DoS)·대량 트래픽 테스트 — 실 운영 서버 대상 테스트는 금지합니다
- 최신 패치가 없는 브라우저/OS 에서만 성립하는 문제
- 소셜 엔지니어링, 물리적 접근
- 취약점 증명을 넘는 실제 교인 데이터 열람·수집·보존

## 보안 관련 문서

- 인증·권한 구조: `CLAUDE.md` 의 Auth / Roles & admin 항목
- 의존성 스캔: CI 의 `security (deps scan)` 잡 (pnpm audit, high 이상 리포트)
