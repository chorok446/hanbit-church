# 기동 시 아래 폴더의 대시보드 JSON 을 자동 로드한다.
apiVersion: 1

providers:
  - name: hanbit
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
