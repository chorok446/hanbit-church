name: Image Verify (develop)

# develop 대상 PR 중 이미지 빌드에 영향 주는 경로가 바뀐 경우에만 Docker 빌드를 검증한다.
# 배경: 프로덕션 이미지 빌드 결함(#62 ISR hang)이 main PR 전용 image-verify 때문에 늦게 발견됐다.
# main 대상 PR 은 기존 image-verify.yml 이 경로 무관 전체 검증한다(릴리스 게이트).

on:
  pull_request:
    branches: [develop]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "apps/api/Dockerfile*"
      - "apps/web/Dockerfile*"
      - "apps/web/next.config.ts"
      - "apps/web/package.json"
      - "apps/web/scripts/validate-build-config.mjs"
      - "apps/web/.env.example"
      - "pnpm-workspace.yaml"
      - "pnpm-lock.yaml"
      - "apps/api/build.gradle.kts"
      - "apps/api/settings.gradle.kts"
      - ".github/workflows/image-verify-develop.yml"

permissions:
  contents: read

concurrency:
  group: image-verify-develop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  image-verify:
    name: ${{ matrix.app }} image
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        include:
          - app: api
            context: apps/api
            file: apps/api/Dockerfile.prod
            build-args: ""
          - app: web
            context: .
            file: apps/web/Dockerfile.prod
            # main 검증과 같은 공개 fixture. 운영 Variables/Secrets 없이 push하지 않는 이미지에만 사용한다.
            build-args: |
              WEB_BUILD_MODE=verification
              NEXT_PUBLIC_API_URL=https://ci-build-placeholder.invalid
              NEXT_PUBLIC_SITE_URL=https://ci-site-placeholder.invalid
              NEXT_PUBLIC_CHURCH_NAME=빌드 검증 교회
              NEXT_PUBLIC_CHURCH_ADDRESS=서울특별시 한빛구 한빛로 1
              NEXT_PUBLIC_CHURCH_PHONE=051-000-0000
    env:
      DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME || 'hanbit-ci-placeholder' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      - id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: docker.io/${{ env.DOCKERHUB_USERNAME }}/hanbit-${{ matrix.app }}
          tags: type=sha,prefix=sha-,format=short

      - uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: ${{ matrix.build-args }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 한국어 잡 요약
        if: always()
        env:
          APP: ${{ matrix.app }}
          RESULT: ${{ job.status }}
        run: |
          {
            echo "## 📦 ${APP} 이미지 빌드 검증"
            echo ""
            if [ "$RESULT" = "success" ]; then
              echo "✅ 프로덕션 Dockerfile 빌드 통과 (push 없음 — 검증 전용)."
            elif [ "$RESULT" = "cancelled" ]; then
              echo "🚫 취소됨 — 새 푸시로 대체되었거나 타임아웃입니다. 빌드 오류가 아닙니다."
            else
              echo "❌ 이미지 빌드 실패 — Dockerfile·빌드 인자·의존성 설치 단계를 확인하세요."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
